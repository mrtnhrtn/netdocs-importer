<Window x:Class="NetDocsImporter.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetDocsImporter.App"
        xmlns:core="clr-namespace:NetDocsImporter.Core;assembly=NetDocsImporter.Core"
        mc:Ignorable="d"
        Title="NetDocs Importer" Height="720" Width="1180" MinHeight="620" MinWidth="980">
    <Window.Resources>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <HierarchicalDataTemplate DataType="{x:Type core:FolderNodeViewModel}" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Opacity" Value="1" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding EffectiveIncluded}" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsEffectivelyEmpty}" Value="True">
                                <Setter Property="Opacity" Value="0.75" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Path Width="14" Height="12" Margin="0,0,4,0" Fill="#F4C542" Stroke="#C89200"
                      Data="M1,4 L9,4 L11,6 L21,6 L21,17 L1,17 Z" />
                <Button Command="{Binding CycleImportModeCommand}" Padding="0" Margin="0,0,6,0" Background="Transparent" BorderThickness="0">
                    <StackPanel Orientation="Horizontal">
                        <Path Width="12" Height="12" Fill="#2E7D32" Data="M2,6 L5,9 L10,2" Stroke="#2E7D32" StrokeThickness="2">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ImportMode}" Value="include">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                        <Path Width="12" Height="12" Stroke="#C62828" StrokeThickness="2" Data="M2,2 L10,10 M10,2 L2,10">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ImportMode}" Value="exclude">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                        <TextBlock Text="?" Foreground="#888888" FontWeight="SemiBold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ImportMode}" Value="inherit">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
                <Path Width="10" Height="10" Margin="0,0,6,0" Fill="#D97706" Stroke="#B45309"
                      Data="M5,0 L10,9 L0,9 Z">
                    <Path.Style>
                        <Style TargetType="Path">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEffectivelyEmpty}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Path.Style>
                    <Path.ToolTip>
                        <ToolTip Content="Empty folder: ndImport will not create this folder because there are no files inside it." />
                    </Path.ToolTip>
                </Path>
                <TextBlock Text="{Binding Name}" Margin="0,0,6,0" TextTrimming="CharacterEllipsis">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#222222" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EffectiveIncluded}" Value="False">
                                    <Setter Property="Foreground" Value="#888888" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsEffectivelyEmpty}" Value="True">
                                    <Setter Property="Foreground" Value="#6B7280" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                    <Setter Property="Foreground" Value="#0B2B5D" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding EffectiveIncluded}" Value="False" />
                                        <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Foreground" Value="#5A6A7A" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="(inherited)" FontStyle="Italic" Foreground="#777777">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsImportInherited}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </HierarchicalDataTemplate>

        <DataTemplate DataType="{x:Type core:FileNodeViewModel}">
            <StackPanel Orientation="Horizontal">
                <Path Width="12" Height="14" Margin="2,0,6,0" Fill="#E0E0E0" Stroke="#999999"
                      Data="M2,1 L10,1 L14,5 L14,18 L2,18 Z M10,1 L10,5 L14,5" />
                <TextBlock Text="{Binding Name}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#333333" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EffectiveIncluded}" Value="False">
                                    <Setter Property="Foreground" Value="#AAAAAA" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F5F7FA" Padding="12" CornerRadius="6" BorderBrush="#E4E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Job Overview" Style="{StaticResource HeaderText}" />
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Job:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding CurrentJobId}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock Text="Source:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding CurrentJobSourceRoot}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock Text="State:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding CurrentJobState}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Select Folder" Width="120" Margin="0,0,6,0" Click="OnSelectFolder"
                                IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBooleanConverter}}"
                                Style="{StaticResource SecondaryButton}" />
                        <Button Content="Cancel Scan" Width="110" Click="OnCancelScan"
                                IsEnabled="{Binding IsScanning}"
                                Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                Style="{StaticResource SecondaryButton}" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Total:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding TotalFilesDisplay}" FontWeight="SemiBold" Margin="0,0,16,0" />
                    <TextBlock Text="Included:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding IncludedFilesCountDisplay}" FontWeight="SemiBold" Margin="0,0,16,0" />
                    <TextBlock Text="Excluded:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ExcludedFilesCountDisplay}" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" MinWidth="260" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="340" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Folders" Style="{StaticResource SectionTitle}" />
                <TextBox Text="{Binding TreeSearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                <Border Background="#F8FAFC" BorderBrush="#D9E2EC" BorderThickness="1" CornerRadius="6" Padding="6">
                    <TreeView ItemsSource="{Binding FolderRoots}"
                              SelectedItemChanged="OnFolderTreeSelectedItemChanged"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              BorderThickness="0">
                        <TreeView.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Include" Click="OnSetImportInclude" />
                                <MenuItem Header="Exclude" Click="OnSetImportExclude" />
                                <MenuItem Header="Clear override" Click="OnSetImportInherit" />
                                <Separator />
                                <MenuItem Header="Apply to children" Click="OnApplyImportToChildren" />
                            </ContextMenu>
                        </TreeView.ContextMenu>
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="Padding" Value="4,3" />
                                <Setter Property="Margin" Value="0,1,0,1" />
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <EventSetter Event="PreviewMouseRightButtonDown" Handler="OnFolderTreeRightButtonDown" />
                                <EventSetter Event="Expanded" Handler="OnFolderTreeExpanded" />
                                <EventSetter Event="Collapsed" Handler="OnFolderTreeCollapsed" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#CFE3FF" />
                                        <Setter Property="BorderBrush" Value="#8BB4FF" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>
                </Border>
            </StackPanel>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          Background="#E4E7EB" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns" />

            <StackPanel Grid.Column="2" Margin="8,0,12,0">
                <TextBlock Text="Files" Style="{StaticResource SectionTitle}" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBox Width="220" Text="{Binding FileSearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                    <Button Content="All" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Click="OnFilterAll" />
                    <Button Content="Included" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Click="OnFilterIncluded" />
                    <Button Content="Excluded" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Click="OnFilterExcluded" />
                    <Button Content="Large" Style="{StaticResource SecondaryButton}" Click="OnFilterLarge" />
                </StackPanel>
                <TextBlock Text="No folders found. Run a scan to populate files."
                           Foreground="#6B7280"
                           Margin="0,0,0,8"
                           Visibility="{Binding HasFolderRoots, Converter={StaticResource InverseBooleanConverter}}" />
                <DataGrid ItemsSource="{Binding FolderFiles}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column"
                          IsReadOnly="True" GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="#CCD0D5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200" />
                        <DataGridTextColumn Header="Relative Path" Binding="{Binding RelativePath}" Width="*" />
                        <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="90" />
                        <DataGridTextColumn Header="Modified" Binding="{Binding ModifiedDisplay}" Width="140" />
                        <DataGridTextColumn Header="Large" Binding="{Binding LargeWarning}" Width="70" />
                        <DataGridTextColumn Header="Import" Binding="{Binding EffectiveImport}" Width="70" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <ScrollViewer Grid.Column="3" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Details" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="{Binding SelectedFolderRelativePath}" FontWeight="SemiBold" Margin="0,0,0,4" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding SelectedFolderPath}" Foreground="#666666" Margin="0,0,0,8" TextWrapping="Wrap" />
                    <TextBlock Text="This folder will not be created in NetDocuments via ndImport."
                               Foreground="#B45309"
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"
                               Visibility="{Binding SelectedFolderIsEffectivelyEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <StackPanel Orientation="Vertical" Margin="0,0,0,8">
                        <TextBlock Text="{Binding SelectedFolderFilesDisplay, StringFormat=Total files: {0}}" />
                        <TextBlock Text="{Binding SelectedFolderBytesDisplay, StringFormat=Total bytes: {0}}" />
                        <TextBlock Text="{Binding SelectedFolderLargeFilesDisplay, StringFormat=Large files: {0}}" />
                        <TextBlock Text="{Binding SelectedFolderExcludedDisplay, StringFormat=Excluded folders: {0}}" />
                    </StackPanel>

                    <TextBlock Text="Import Mode" Style="{StaticResource SectionTitle}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Button Width="96" Margin="0,0,6,0" Click="OnSetImportInherit" Style="{StaticResource SecondaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="?" Foreground="#888888" Margin="0,0,6,0" />
                                <TextBlock Text="Inherit" />
                            </StackPanel>
                        </Button>
                        <Button Width="96" Margin="0,0,6,0" Click="OnSetImportInclude" Style="{StaticResource SecondaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <Path Width="12" Height="12" Fill="#2E7D32" Data="M2,6 L5,9 L10,2" Stroke="#2E7D32" StrokeThickness="2" Margin="0,0,6,0" />
                                <TextBlock Text="Include" />
                            </StackPanel>
                        </Button>
                        <Button Width="96" Click="OnSetImportExclude" Style="{StaticResource SecondaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <Path Width="12" Height="12" Stroke="#C62828" StrokeThickness="2" Data="M2,2 L10,10 M10,2 L2,10" Margin="0,0,6,0" />
                                <TextBlock Text="Exclude" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <TextBlock Text="{Binding SelectedImportMode, StringFormat=Selected mode: {0}}" Foreground="#555555" />
                    <TextBlock Text="{Binding SelectedEffectiveImportMode, StringFormat=Effective mode: {0}}" Foreground="#555555" Margin="0,0,0,12" />

                    <TextBlock Text="Import Controls" Style="{StaticResource SectionTitle}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Start Import" Width="100" Margin="0,0,6,0" Click="OnStartImport"
                                IsEnabled="{Binding CanStartImport}" Style="{StaticResource PrimaryButton}" />
                        <Button Content="Pause" Width="70" Margin="0,0,6,0" Click="OnPauseImport"
                                IsEnabled="{Binding CanPauseImport}" Style="{StaticResource SecondaryButton}" />
                        <Button Content="Resume" Width="70" Margin="0,0,6,0" Click="OnResumeImport"
                                IsEnabled="{Binding CanResumeImport}" Style="{StaticResource SecondaryButton}" />
                        <Button Content="Cancel" Width="70" Click="OnCancelImport"
                                IsEnabled="{Binding CanCancelImport}" Style="{StaticResource SecondaryButton}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Open Logs Folder" Width="130" Margin="0,0,6,0" Click="OnOpenLogs" Style="{StaticResource SecondaryButton}" />
                        <Button Content="Open Reports Folder" Width="140" Click="OnOpenReports" Style="{StaticResource SecondaryButton}" />
                    </StackPanel>

                    <TextBlock Text="ndImport" Style="{StaticResource SectionTitle}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Button Content="Export for ndImport..." Width="160" Margin="0,0,6,0" Click="OnExportNdImport" Style="{StaticResource SecondaryButton}" />
                        <Button Content="Launch ndImport" Width="130" Click="OnLaunchNdImport" Style="{StaticResource PrimaryButton}" />
                    </StackPanel>
                    <TextBlock Text="Path to ndimport.exe" Foreground="#555555" />
                    <TextBox Text="{Binding NdImportPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6" />
                    <TextBlock Text="Host" Foreground="#555555" />
                    <ComboBox SelectedValue="{Binding NdImportHost}" SelectedValuePath="Content" Margin="0,2,0,6">
                        <ComboBoxItem Content="upload.au.netdocuments.com" />
                        <ComboBoxItem Content="upload.eu.netdocuments.com" />
                        <ComboBoxItem Content="upload.vault.netvoyage.com" />
                    </ComboBox>
                    <TextBlock Text="Cabinet" Foreground="#555555" />
                    <TextBox Text="{Binding NdImportCabinet, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6" />
                    <TextBlock Text="Username" Foreground="#555555" />
                    <TextBox Text="{Binding NdImportUsername, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6" />
                    <TextBlock Text="Password (optional, not saved)" Foreground="#555555" />
                    <TextBox Text="{Binding NdImportPassword, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,4" />
                    <CheckBox Content="Include password on CLI (sensitive)" IsChecked="{Binding NdImportIncludePassword}" Margin="0,0,0,6" />
                    <TextBlock Text="Warning: CLI passwords are sensitive. Prefer ndImport's prompt when possible." Foreground="#9F6000" TextWrapping="Wrap" Margin="0,0,0,6" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <CheckBox Content="/utf8=Y" IsChecked="{Binding NdImportUtf8}" Margin="0,0,10,0" />
                        <CheckBox Content="/dateformat=Y" IsChecked="{Binding NdImportDateFormat}" Margin="0,0,10,0" />
                        <CheckBox Content="/noval=Y" IsChecked="{Binding NdImportNoValidation}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="/maxerr" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBox Width="70" Text="{Binding NdImportMaxErrors, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <TextBlock Text="Profiling" Style="{StaticResource SectionTitle}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <RadioButton Content="Inherit" GroupName="ProfileMode" Margin="0,0,12,0"
                                     Checked="OnProfileInheritChecked">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Setter Property="IsChecked" Value="False" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedProfileMode}" Value="inherit">
                                            <Setter Property="IsChecked" Value="True" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                        <RadioButton Content="Override" GroupName="ProfileMode"
                                     Checked="OnProfileOverrideChecked">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Setter Property="IsChecked" Value="False" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedProfileMode}" Value="override">
                                            <Setter Property="IsChecked" Value="True" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                    </StackPanel>
                    <TextBlock Text="{Binding SelectedProfileSource, StringFormat=Profile source: {0}}" Foreground="#555555" Margin="0,0,0,6" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Button Content="Add field" Width="90" Margin="0,0,6,0" Click="OnAddProfileField" Style="{StaticResource SecondaryButton}" />
                        <Button Content="Remove field" Width="110" Margin="0,0,6,0" Click="OnRemoveProfileField" Style="{StaticResource SecondaryButton}" />
                        <Button Content="Apply to children" Width="130" Click="OnApplyProfileToChildren" Style="{StaticResource SecondaryButton}" />
                    </StackPanel>
                    <ListView ItemsSource="{Binding ProfileFields}" SelectedItem="{Binding SelectedProfileField, Mode=TwoWay}" Height="140">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Key" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Value" Width="160">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <TextBlock Text="ndImport Sessions" Style="{StaticResource SectionTitle}" />
                    <ListView ItemsSource="{Binding NdImportSessions}" Height="140" Margin="0,0,0,10">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Started" Width="110" DisplayMemberBinding="{Binding StartedDisplay}" />
                                <GridViewColumn Header="Status" Width="110" DisplayMemberBinding="{Binding StatusDisplay}" />
                                <GridViewColumn Header="Details" Width="160" DisplayMemberBinding="{Binding Details}" />
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <TextBlock Text="Recent Jobs" Style="{StaticResource SectionTitle}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Button Content="Load Recent" Width="110" Click="OnLoadRecentJobs" Style="{StaticResource SecondaryButton}" />
                    </StackPanel>
                    <ListView ItemsSource="{Binding RecentJobs}" SelectedItem="{Binding SelectedRecentJob, Mode=TwoWay}" Height="160">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Job" Width="70" DisplayMemberBinding="{Binding JobIdShort}" />
                                <GridViewColumn Header="Created" Width="110" DisplayMemberBinding="{Binding CreatedDisplay}" />
                                <GridViewColumn Header="Status" Width="90" DisplayMemberBinding="{Binding Status}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E4E7EB" BorderThickness="1" Padding="8">
            <DockPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="#52606D" DockPanel.Dock="Left" />
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock Text="Included:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding IncludedFilesCountDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Excluded:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ExcludedFilesCountDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Succeeded:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ImportSucceededDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Failed:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ImportFailedDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Throughput:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ImportThroughputDisplay}" FontWeight="SemiBold" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
