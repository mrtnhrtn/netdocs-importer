<Window x:Class="NetDocsImporter.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetDocsImporter.App"
        xmlns:core="clr-namespace:NetDocsImporter.Core;assembly=NetDocsImporter.Core"
        xmlns:steps="clr-namespace:NetDocsImporter.App.Views.Steps"
        mc:Ignorable="d"
        Title="NetDocs Importer" Height="720" Width="1180" MinHeight="620" MinWidth="980">
    <Window.Resources>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <HierarchicalDataTemplate DataType="{x:Type core:FolderNodeViewModel}" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Opacity" Value="1" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding EffectiveIncluded}" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsEffectivelyEmpty}" Value="True">
                                <Setter Property="Opacity" Value="0.75" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Path Width="14" Height="12" Margin="0,0,4,0" Fill="#F4C542" Stroke="#C89200"
                      Data="M1,4 L9,4 L11,6 L21,6 L21,17 L1,17 Z" />
                <Button Command="{Binding CycleImportModeCommand}" Padding="0" Margin="0,0,6,0" Background="Transparent" BorderThickness="0">
                    <StackPanel Orientation="Horizontal">
                        <Path Width="12" Height="12" Fill="#2E7D32" Data="M2,6 L5,9 L10,2" Stroke="#2E7D32" StrokeThickness="2">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ImportMode}" Value="include">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                        <Path Width="12" Height="12" Stroke="#C62828" StrokeThickness="2" Data="M2,2 L10,10 M10,2 L2,10">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ImportMode}" Value="exclude">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                        <TextBlock Text="?" Foreground="#888888" FontWeight="SemiBold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ImportMode}" Value="inherit">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
                <Path Width="10" Height="10" Margin="0,0,6,0" Fill="#D97706" Stroke="#B45309"
                      Data="M5,0 L10,9 L0,9 Z">
                    <Path.Style>
                        <Style TargetType="Path">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEffectivelyEmpty}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Path.Style>
                    <Path.ToolTip>
                        <ToolTip Content="Empty folder: ndImport will not create this folder because there are no files inside it." />
                    </Path.ToolTip>
                </Path>
                <TextBlock Text="{Binding Name}" Margin="0,0,6,0" TextTrimming="CharacterEllipsis">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#222222" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EffectiveIncluded}" Value="False">
                                    <Setter Property="Foreground" Value="#888888" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsEffectivelyEmpty}" Value="True">
                                    <Setter Property="Foreground" Value="#6B7280" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                    <Setter Property="Foreground" Value="#0B2B5D" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding EffectiveIncluded}" Value="False" />
                                        <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Foreground" Value="#5A6A7A" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="(inherited)" FontStyle="Italic" Foreground="#777777">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsImportInherited}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </HierarchicalDataTemplate>

        <DataTemplate DataType="{x:Type core:FileNodeViewModel}">
            <StackPanel Orientation="Horizontal">
                <Path Width="12" Height="14" Margin="2,0,6,0" Fill="#E0E0E0" Stroke="#999999"
                      Data="M2,1 L10,1 L14,5 L14,18 L2,18 Z M10,1 L10,5 L14,5" />
                <TextBlock Text="{Binding Name}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#333333" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EffectiveIncluded}" Value="False">
                                    <Setter Property="Foreground" Value="#AAAAAA" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F5F7FA" Padding="12" CornerRadius="6" BorderBrush="#E4E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Job Overview" Style="{StaticResource HeaderText}" />
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Job:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding CurrentJobId}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock Text="Source:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding CurrentJobSourceRoot}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock Text="State:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding CurrentJobState}" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Total:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding TotalFilesDisplay}" FontWeight="SemiBold" Margin="0,0,16,0" />
                    <TextBlock Text="Included:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding IncludedFilesCountDisplay}" FontWeight="SemiBold" Margin="0,0,16,0" />
                    <TextBlock Text="Excluded:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ExcludedFilesCountDisplay}" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

                <Grid Grid.Row="1" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" MinWidth="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F8FAFC" BorderBrush="#E4E7EB" BorderThickness="1" CornerRadius="6" Padding="8">
                <ListBox ItemsSource="{Binding Steps}" SelectedItem="{Binding CurrentStep, Mode=TwoWay}" BorderThickness="0" Background="Transparent">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="4" />
                            <Setter Property="Margin" Value="0,2" />
                            <Setter Property="IsEnabled" Value="{Binding IsEnabled}" />
                            <Setter Property="Background" Value="Transparent" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E9EDF2" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:StepItem}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="22" Height="22" Background="#E1E7EF" CornerRadius="11" Margin="0,0,8,0">
                                        <TextBlock Text="{Binding Number}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" />
                                    </Border>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                </StackPanel>
                                <TextBlock Text="{Binding Subtitle}" Foreground="#667085" TextWrapping="Wrap" Margin="30,2,0,0" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Column="1" Background="White" BorderBrush="#E4E7EB" BorderThickness="1" CornerRadius="6" Padding="12" Margin="12,0,0,0">
                <ContentControl Content="{Binding}">
                    <ContentControl.Resources>
                        <DataTemplate x:Key="SelectFolderTemplate">
                            <steps:SelectFolderStepView />
                        </DataTemplate>
                        <DataTemplate x:Key="ReviewScopeTemplate">
                            <steps:ReviewScopeStepView />
                        </DataTemplate>
                        <DataTemplate x:Key="ProfilingTemplate">
                            <steps:ProfilingStepView />
                        </DataTemplate>
                        <DataTemplate x:Key="NdImportConfigTemplate">
                            <steps:NdImportConfigStepView />
                        </DataTemplate>
                        <DataTemplate x:Key="RunImportTemplate">
                            <steps:RunImportStepView />
                        </DataTemplate>
                        <DataTemplate x:Key="RecentJobsTemplate">
                            <steps:RecentJobsStepView />
                        </DataTemplate>
                    </ContentControl.Resources>
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Setter Property="ContentTemplate" Value="{StaticResource SelectFolderTemplate}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentStep.Key}" Value="{x:Static local:StepKey.SelectFolder}">
                                    <Setter Property="ContentTemplate" Value="{StaticResource SelectFolderTemplate}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentStep.Key}" Value="{x:Static local:StepKey.ReviewScope}">
                                    <Setter Property="ContentTemplate" Value="{StaticResource ReviewScopeTemplate}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentStep.Key}" Value="{x:Static local:StepKey.Profiling}">
                                    <Setter Property="ContentTemplate" Value="{StaticResource ProfilingTemplate}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentStep.Key}" Value="{x:Static local:StepKey.NdImportConfig}">
                                    <Setter Property="ContentTemplate" Value="{StaticResource NdImportConfigTemplate}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentStep.Key}" Value="{x:Static local:StepKey.RunImport}">
                                    <Setter Property="ContentTemplate" Value="{StaticResource RunImportTemplate}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentStep.Key}" Value="{x:Static local:StepKey.RecentJobs}">
                                    <Setter Property="ContentTemplate" Value="{StaticResource RecentJobsTemplate}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E4E7EB" BorderThickness="1" Padding="8">
            <DockPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="#52606D" DockPanel.Dock="Left" />
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock Text="Included:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding IncludedFilesCountDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Excluded:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ExcludedFilesCountDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Succeeded:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ImportSucceededDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Failed:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ImportFailedDisplay}" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <TextBlock Text="Throughput:" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding ImportThroughputDisplay}" FontWeight="SemiBold" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>

