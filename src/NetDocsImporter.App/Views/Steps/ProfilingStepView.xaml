<UserControl x:Class="NetDocsImporter.App.Views.Steps.ProfilingStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <StackPanel>
        <TextBlock Text="Profiling" Style="{StaticResource SectionTitle}" />
        <Border BorderBrush="#E4E7EB" BorderThickness="1" CornerRadius="6" Padding="10">
            <StackPanel>
                <TextBlock Text="Profiling setup moved earlier in the workflow." FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBlock Text="Use Select Folder to connect to NetDocuments, choose source content, select the upload target, and sync inherited profile defaults."
                           TextWrapping="Wrap"
                           Foreground="#555555" />
                <TextBlock Text="Legacy schema-loading and override controls are deprecated in this version."
                           Margin="0,6,0,0"
                           Foreground="#B45309" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
