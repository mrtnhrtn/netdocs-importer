<UserControl x:Class="NetDocsImporter.App.Views.Steps.RunImportStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <StackPanel>
        <TextBlock Text="Import Controls" Style="{StaticResource SectionTitle}" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Start Import" Width="100" Margin="0,0,6,0" Click="OnStartImport"
                    IsEnabled="{Binding CanStartImport}" Style="{StaticResource PrimaryButton}" />
            <Button Content="Pause" Width="70" Margin="0,0,6,0" Click="OnPauseImport"
                    IsEnabled="{Binding CanPauseImport}" Style="{StaticResource SecondaryButton}" />
            <Button Content="Resume" Width="70" Margin="0,0,6,0" Click="OnResumeImport"
                    IsEnabled="{Binding CanResumeImport}" Style="{StaticResource SecondaryButton}" />
            <Button Content="Cancel" Width="70" Click="OnCancelImport"
                    IsEnabled="{Binding CanCancelImport}" Style="{StaticResource SecondaryButton}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Open Logs Folder" Width="130" Margin="0,0,6,0" Click="OnOpenLogs" Style="{StaticResource SecondaryButton}" />
            <Button Content="Open Reports Folder" Width="140" Click="OnOpenReports" Style="{StaticResource SecondaryButton}" />
        </StackPanel>

        <TextBlock Text="ndImport Sessions" Style="{StaticResource SectionTitle}" />
        <ListView ItemsSource="{Binding NdImportSessions}" Height="180" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Started" Width="110" DisplayMemberBinding="{Binding StartedDisplay}" />
                    <GridViewColumn Header="Status" Width="110" DisplayMemberBinding="{Binding StatusDisplay}" />
                    <GridViewColumn Header="Details" Width="160" DisplayMemberBinding="{Binding Details}" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
